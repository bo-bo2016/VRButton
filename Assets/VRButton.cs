using UnityEngine;

public class VRButton : MonoBehaviour
{
    public Color hightlightColor = new Color(225f / 255f, 200f / 255f, 150f / 255f, 1f);
    public Color normalColor = new Color(225f / 255f, 200f / 255f, 150f / 255f, 1f);

    // Use this for initialization
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}